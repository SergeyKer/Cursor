/**
 * Запуск локального сервера с фиксированным портом 3000 (если PORT не задан в окружении).
 * Снижает риск открыть в браузере :3000, пока npm run dev слушает другой порт.
 */
if (!process.env.PORT) {
  process.env.PORT = '3000';
}

require('./local-dev.js');
